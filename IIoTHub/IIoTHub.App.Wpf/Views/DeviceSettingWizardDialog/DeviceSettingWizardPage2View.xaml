<UserControl x:Class="IIoTHub.App.Wpf.Views.DeviceSettingWizardDialog.DeviceSettingWizardPage2View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 標題 -->
        <StackPanel Orientation="Vertical" Margin="0,0,0,20">
            <TextBlock FontSize="20"
                       FontWeight="Bold"
                       Foreground="{StaticResource PrimaryBrush}">
                <Run Text="{Binding DriverDisplayName, Mode=OneWay}"/>
                <Run Text=" - 通訊設定"/>
            </TextBlock>
            <Border Height="2" Background="{StaticResource PrimaryBrush}" Margin="0,6,0,0"/>
        </StackPanel>

        <!-- 動態內容 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,12,0">
            <ItemsControl ItemsSource="{Binding ConnectionSettings}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource RowCardStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- 名稱 -->
                                <TextBlock Text="{Binding DisplayName}"
                                           Grid.Column="0"
                                           VerticalAlignment="Center"
                                           FontWeight="SemiBold"
                                           FontSize="14"
                                           Foreground="#222"
                                           Padding="8,0"/>

                                <!-- 值 -->
                                <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                         Grid.Column="1"
                                         Style="{StaticResource ModernTextBoxStyle}"
                                         Margin="4,0"/>

                                <!-- 備註 -->
                                <TextBlock Text="{Binding Note}"
                                           Grid.Column="2"
                                           VerticalAlignment="Center"
                                           Foreground="#555"
                                           FontSize="13"
                                           Padding="8,0"/>
                                
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        
    </Grid>
    
</UserControl>